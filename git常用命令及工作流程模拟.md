# git常用命令

| 场景             | 命令                                               |
| ---------------- | -------------------------------------------------- |
| 克隆远程仓库     | git clone 远程仓库地址                             |
| 初始化本地仓库   | git init                                           |
| 关联远程仓库     | git remote add origin 远程仓库地址                 |
| 产看工作状态     | git status                                         |
| 加入暂存区       | git add . / git add 文件名                         |
| 提交到版本库     | git commit -m "说明原因"                           |
| 拉取远程最新代码 | git pull origin 分支名                             |
| 推送到远程仓库   | git push origin 分支名 / git push -u origin 分支名 |
| 查看简介提交历史 | git log --oneline                                  |



### git核心流程：

​	[拉取最新代码→修改→暂存→提交→再拉取→推送]





## 模拟工作全流程（分步执行，全程验证 Git 操作）

#### 阶段 1：初始化仓库 & 准备基础环境（对应工作中「新建文档项目」）

#### **1、本地创建文档项目文件夹**

打开 Git Bash，创建并进入文件夹（比如 api-doc-xx-product）：

```
# 创建文件夹
mkdir api-doc-xx-product
# 进入文件夹
cd api-doc-xx-product
```

1. ##### 初始化本地 Git 仓库

   ```
   git init
   ```

   执行后，文件夹内会生成隐藏的 `.git` 目录（版本库创建完成）。

2. ##### 关联远程仓库（模拟团队共享仓库）

   把本地文档仓库关联到你的远程仓库（替换为你自己的远程仓库地址）：

   ```
   git remote add origin git@github.com:地址
   ```

3. ##### 创建并切换到文档专用分支（核心：不直接操作 main 分支）

   推荐分支命名：`doc/xx-product-api-v1.0`（清晰标识「文档 / 产品 / 版本」），命令如下：

   ```
   git checkout -b doc/xx-product-api-v1.0
   ```

   执行后终端提示 `Switched to a new branch 'doc/xx-product-api-v1.0'`，分支创建成功。

### 阶段 2：文档编写 & 本地版本提交（对应工作中「分阶段编写 / 更新文档」）

这个阶段模拟你「逐步完善文档」的过程，核心是「小步提交」，每完成一个小模块就提交一次，方便后续追溯修改记录。

#### 第一步：创建基础文档文件（v1.0 文档初稿）

在 `api-doc-xx-product` 文件夹内，新建 2 个文件（可用记事本 / VS Code 编辑）：

- `README.md`：文档说明（比如「XX 产品 v1.0 接口文档，适用于开发人员对接」）

- ```
  api-v1.0.md
  ```

  ：核心接口文档，先写基础结构：

  ```
  # XX 产品 v1.0 接口文档
  ## 1. 文档说明
  本文档包含 XX 产品 v1.0 所有公开接口的请求地址、参数、返回值说明。
  ## 2. 基础请求信息
  - 网关地址：https://api.xx-product.com/v1
  - 请求方式：POST/GET
  - 数据格式：JSON
  ```

保存文件后，回到 Git Bash。



#### **第二步：查看工作区状态 & 提交第一次版本（初稿完成）**



```
# 1. 查看当前修改状态（确认新建的文件）
git status
# 此时会看到 2 个红色文件（未加入暂存区）

# 2. 将所有文件加入暂存区
git add .

# 3. 提交到本地版本库（提交信息要贴合你的工作，清晰描述）
git commit -m "doc: 完成 XX 产品 v1.0 接口文档基础结构搭建"
```

提交成功后，终端会显示提交记录（包含修改的文件数、提交哈希）。

#### 第三步：更新文档 & 提交第二次版本（新增接口详情）

编辑 api-v1.0.md，新增一个登录接口的详情（模拟你完善文档内容）：

```
XX 产品 v1.0 接口文档
## 1. 文档说明
本文档包含 XX 产品 v1.0 所有公开接口的请求地址、参数、返回值说明。
## 2. 基础请求信息
- 网关地址：https://api.xx-product.com/v1
- 请求方式：POST/GET
- 数据格式：JSON
## 3. 核心接口详情
### 3.1 登录接口
- 接口地址：`/user/login`
- 请求方式：POST
- 请求参数：

  | 参数名   | 类型   | 必填 | 说明             |
  | -------- | ------ | ---- | ---------------- |
  | username | string | 是   | 用户名           |
  | password | string | 是   | 密码（MD5 加密） |
```

保存文件后，回到 Git Bash 提交版本： 

```
# 1. 查看修改状态

git status

# 2. 暂存 + 提交（小步提交，清晰描述更新内容）

git add .
git commit -m "doc: 新增 v1.0 登录接口详情，补充参数说明"
```



#### 第四步：优化文档 & 提交第三次版本（修改格式 / 补充示例，模拟文档校对优化）

继续编辑 api-v1.0.md，给登录接口补充返回值示例（模拟你校对后完善内容）：

保存后提交版本：

```
git add .
git commit -m "doc: 补充登录接口成功返回示例，优化格式排版"
```

查看本地提交历史（验证版本是否记录完整）
执行命令查看你刚才的三次提交，确认版本记录清晰：

```
git log --oneline
```

输出会类似这样（哈希值不同，提交信息对应你的三次操作）：

```
plaintext
a1b2c3d (HEAD -> doc/xx-product-api-v1.0) doc: 补充登录接口成功返回示例，优化格式排版
d3e4f5g doc: 新增 v1.0 登录接口详情，补充参数说明
g6h7i8j doc: 完成 XX 产品 v1.0 接口文档基础结构搭建
```

这说明你本地的文档版本已经完整记录，符合工作中的版本控制要求。

### 阶段 3：推送文档分支到远程（对应工作中「共享文档给团队」）

本地文档编写完善后，需要推送到远程仓库，让产品、开发同事可以查看和使用，步骤如下：

#### 1.推送本地文档分支到远程仓库

执行命令推送 doc/xx-product-api-v1.0 分支到远程，并建立关联（后续可直接 git push/pull）：

```
git push -u origin doc/xx-product-api-v1.0
```

#### 2.验证远程仓库（确认推送成功）

打开你的远程仓库页面（比如 GitHub 的 zzrise/vansi），刷新后会看到：
多了一个 doc/xx-product-api-v1.0 分支。
该分支下有 README.md 和 api-v1.0.md 两个文件，内容和你本地一致。
这说明推送成功，你已经完成了「本地文档 → 远程共享」的核心流程。

### 阶段 4：文档迭代（v1.1 补充内容）& 合并回主分支（对应工作中「文档迭代更新」）

模拟产品迭代，需要给文档补充「错误码说明」（v1.1 版本），完成后合并到 main 分支（主分支用于存放最终版 / 正式版文档）。

#### 1、确保当前在文档分支，拉取远程最新（模拟团队无其他修改，仅做流程验证）


```
# 确认在 doc/xx-product-api-v1.0 分支（如果不在，执行 git checkout 切换）

git checkout doc/xx-product-api-v1.0

# 拉取远程该分支最新代码（模拟团队无其他修改，确保本地与远程同步）

git pull origin doc/xx-product-api-v1.0
```

#### 2、迭代文档（补充 v1.1 错误码说明）& 提交版本

编辑 api-v1.0.md（也可以重命名为 api-v1.1.md，模拟版本迭代），在文档末尾补充：

```
## 4. 通用错误码说明（v1.1 新增）
| 错误码 | 说明 | 解决方案 | 
 |--------|------|----------|
 | 200 | 操作成功 | - | 
 | 400 | 参数错误 | 检查请求参数是否符合要求 |
 | 401 | 未授权/Token 过期 | 重新登录获取有效 Token | 
 | 500 | 服务器内部错误 | 联系后端开发人员排查 |
```

 保存后提交迭代版本：

```
git add . 
git commit -m "doc: 迭代 v1.1，补充通用错误码说明及解决方案"
```

#### 3.推送迭代后的分支到远程  

```
 git push # 因为之前已经用 -u 建立关联，直接 git push 即可 
```

#### 4.切换回 main 分支 & 拉取远程最新                     

合并分支前，先确保 main 分支是最新状态：    

```
#切换回main分支
git checkout main

# 拉取远程 main 分支最新代码

git pull origin main
```

#### 5.将文档分支合并到 main 分支（正式版文档归档）

执行合并命令，将 doc/xx-product-api-v1.0 的所有文档内容合并到 main 分支：

```
git merge doc/xx-product-api-v1.0
```

#### 6.推送合并后的 main 分支到远程（正式版归档共享）

```
git push origin main
```

#### 7.验证结果

打开远程仓库的 main 分支，会看到 README.md 和 api-v1.0.md（包含 v1.1 错误码）已经存在，这说明文档迭代和合并归档完成，符合工作中的正式版文档管理要求。

### 阶段 5：清理无用分支（可选，对应工作中「项目收尾」）

文档已经合并到 main 分支，且正式归档，可清理本地和远程的 doc/xx-product-api-v1.0 分支（避免分支冗余）：

```
1. 本地删除该分支（-d 安全删除，仅删除已合并的分支）
   git branch -d doc/xx-product-api-v1.0
2. 远程删除该分支（确认团队无需求后执行）
   git push origin --delete doc/xx-product-api-v1.0
```

